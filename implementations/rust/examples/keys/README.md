# Example Keys (DO NOT USE IN PRODUCTION)

These RSA keys are included **only** to let the reference implementation run out-of-the-box.

- `provider_rsa_private.pem`
- `provider_rsa_public.pem`

In production, you must:
- generate your own keys
- store them in an HSM / key vault
- rotate regularly
